{% extends "LapaperieAgendaBundle:Admin:form.html.twig" %}

  {% block action %}
<form action="{{ path('agenda_edit', { 'id': entity.id }) }}" method="post" {{ form_enctype(form) }}>
  {% endblock %}

  {% block legend %}Actualité{% endblock %}

  {% block button %}Enregistrer les modifications{% endblock %}

  {% block delete %}
  <div class="row">
    <div class="span5 offset12">
<form action="{{ path('agenda_delete', { 'id': entity.id }) }}" method="post">
{{ form_widget(delete_form) }}
            <button type="submit" class="btn danger confirm">Supprimer cette Actualité</button>
</form>
    </div>
  </div>
  {% endblock %}
