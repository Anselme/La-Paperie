{% extends "LapaperieCompaniesBundle::layout.html.twig" %}

{% block content %}

<h1>Focus</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Titre</th>
            <th>Edito</th>
            <th>En ligne</th>
            <th>Date de publication</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('focus_edit', { 'id': entity.id }) }}">{{ entity.title }}</a></td>
            <td>{{ entity.text | truncate(100) }}</td>
            <td>
            {% if entity.isOnLine %}
            <img width="16" height="16" src="{{ asset('bundles/lapaperimain/images/icons/tick.png') }}" />
            {% else %}
            <img width="16" height="16" src="{{ asset('bundles/lapaperiemain/images/icons/cancel.png') }}" />
            {% endif %}
            </td>
            <td>{{ entity.publicationDate|date('j M Y') }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<div class="button">
        <a href="{{ path('focus_new') }}">
        Ecrire un nouvel Edito
        </a>
</div>
{% endblock %}
